stages:
  - test

pytest:
  image: python:3.12

  stage: test

  before_script:
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt

  script:
    - python -m pytest -v --junitxml=report.xml

  artifacts:
    when: always
    reports:
      junit: report.xml